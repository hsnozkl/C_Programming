#include <stdio.h>
#include <stdlib.h>

int main()
{
    // kullanýcýdan bir sayý girmesini isteyiniz if else kullanarak sayýyý tek mi çift mi olduðunu
    // ekrana bastýran bir program yazýnýz

    /*int sayim;

    printf("Lutfen bir sayi girirniz\n");
    scanf("%d",&sayim);

    if(sayim%2 == 0)
    {
        printf("%d sayisi cift sayidir",sayim);
    }
    else
    {
        printf("%d sayisi tek sayidir",sayim);
    }

    return 0; */

    //kiþinin saçimlerde yasal olarak oy kullanýp kullanamayacaðýný ekrana
    //bastýran program yazýnýz

    /*int yasim;

    printf("Lutfen yasinizi giriniz\n");
    scanf("%d",&yasim);

    if(yasim >= 18)
    {
        printf("Yasal olarak oy kullanabilirsin",yasim);
    }
    else
    {
        printf("Yasal olarak oy kullanamazsin",yasim);
    }

    return 0; */

    //kullanýcýdan 2 adet sayý alýnýz büyüklük küçüklük durumuna
    //göre ekrana bastýran iþlem yapýnýz

    /*int a,b;

    printf("Lutfen 2 adet sayi giriniz\n");
    scanf("%d %d",&a,&b);

    if(a == b)
    {
        printf("Sonuc : %d = %d",a,b);
    }
    else if(a > b)
    {
        printf("Sonuc : %d > %d",a,b);
    }
    else
    {
        printf("Sonuc : %d < %d",a,b);
    }

    return 0;*/

    /* int a,b;

    printf("Lutfen 2 adet sayi giriniz\n");
    scanf("%d %d",&a,&b);

    if(a >= b)
    {
        if(a == b)
        {
            printf("Sonuc : %d = %d",a,b);
        }
        else
        {
            printf("Sonuc : %d > %d",a,b);
        }
    }
    else
    {
        printf("Sonuc : %d < %d",a,b);
    }

    return 0;*/


}
